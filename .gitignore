# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Project artifacts (large / regenerable). Anchored with leading `/` so the
# `data/` rule doesn't accidentally swallow `src/data/`.
/models/
/data/
/runs/
*.pt
*.pth
*.ckpt
*.pgn

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
